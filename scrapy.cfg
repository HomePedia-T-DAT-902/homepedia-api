# Scrapy project configuration.
# Run scrapers from the repo root, e.g.:
#   scrapy crawl villeideale
#   scrapy crawl villeideale -a depts=49 -a max_cities=2 -a max_review_pages=2
[settings]
default = src.scraping.ville_ideale.settings
